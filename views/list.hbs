<div>
    {{#each posts}}
    <h1>{{ title }}</h1>
    <p>{{ message }}</p>
    <p>{{ startDate }}</p>
    <p>{{ endDate }}</p>

    <p>Are you interested in providing this service?</p>
        <button>Contact the Pet Owner</button>
    {{/each}}

</div>

